// @flow

import { createAction } from 'redux-actions';

import { APPLICATION_SETTINGS_THEME_CHANGED } from '../constants';

export const ApplicationSettingsThemeChanged = createAction(APPLICATION_SETTINGS_THEME_CHANGED);
